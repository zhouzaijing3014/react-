/**
 * 用于创建包含N个action对象的工厂函数
 */

 /**
  * 创建增加的action
  */
    export  const increment = 1(number)=>({type:'INCREMENT',number})

  /**
   * 创建删除的action   返回action的工厂函数
   */
   
    export  const decrement = (number)=>({type:'DECRMENT',number})
   